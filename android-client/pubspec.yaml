name: polaris
description: Polaris VPN client for Android — delay test, location, security, Xray tunnel.
publish_to: "none"
version: 1.3.0+13

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_v2ray_client: ^3.5.0
  http: ^1.2.2
  shared_preferences: ^2.3.3
  uuid: ^4.5.1

flutter:
  uses-material-design: true
