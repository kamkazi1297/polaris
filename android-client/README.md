# Polaris Android

Flutter client: delay test, location, security, subscriptions, duplicate removal, sort-by-valid, Xray VPN via `flutter_v2ray_client`.

Built by GitHub Actions (`.github/workflows/release.yml`) into `Polaris.apk`.
